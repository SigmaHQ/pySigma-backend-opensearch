[tool.poetry]
name = "pySigma-backend-opensearch"
version = "0.1.0"
description = "(OrderedDict([('target_name', 'Opensearch'), ('backend_package_name', 'opensearch'), ('backend_name', 'Opensearch'), ('backend_class_name', 'OpensearchLuceneBackend'), ('package_type', 'backend'), ('package_name', 'pySigma-backend-opensearch'), ('package_description', 'pySigma Opensearch backend'), ('author', 'Hendrik Baecker'), ('email', 'hendrik.baecker@dcso.de'), ('license', 'LGPL-3.0-only'), ('github_account', 'SigmaHQ'), ('test_badge', 'True'), ('coverage_badge', 'True'), ('coverage_gist', 'e95ff0904786bd5883f19105b6a3a1ee'), ('status_badge', 'True'), ('status', 'pre--release-orange'), ('additional_output_formats', 'False'), ('output_formats', 'lucene,dsl_lucene,monitor_rule'), ('_template', 'https://github.com/SigmaHQ/cookiecutter-pySigma-backend.git')]), Undefined)"
authors = ["Hendrik Baecker <hendrik.baecker@dcso.de>"]
license = "LGPL-3.0-only"
repository = "https://github.com/SigmaHQ/pySigma-backend-opensearch"
packages = [
    { include = "sigma" }
]

[tool.poetry.dependencies]
python = "^3.8"
pysigma-backend-elasticsearch = {git = "https://github.com/SigmaHQ/pySigma-backend-elasticsearch.git", rev = "main"}
pysigma = "^0.7.1"

[tool.poetry.dev-dependencies]
pytest = "^7.1.2"
autopep8 = "^1.6.0"
pytest-cov = "^3.0.0"
coverage = "^6.4.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
